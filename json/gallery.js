import galleryData from './galley.json';

export const images = galleryData;

// If you need to modify or process the data before exporting, you can do so here
// For example:
// export const images = galleryData.map(image => ({
//   ...image,
//   img_url: `/images/${image.img_url}`
// })); 